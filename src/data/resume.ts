import { portfolioData } from './portfolioData';

/**
 * Re-export portfolioData for backward compatibility across components.
 * To update your portfolio information, edit `src/data/portfolioData.ts`!
 */
export const resumeData = portfolioData;
